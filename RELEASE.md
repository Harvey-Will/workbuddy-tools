# WorkBuddy Tools v0.1.0

This folder is the **clean first public release tree** (no git history).

- Version: **0.1.0**
- License: MIT
- No local paths, account IDs, or debug scripts

Push as a brand-new repo:

```bash
cd workbuddy-tools-oss
git init
git add .
git commit -m "chore: WorkBuddy Tools v0.1.0"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main
```
